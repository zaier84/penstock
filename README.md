# penstock

Composable, testable backend workflows for Node.js — use-cases, pipelines,
steps, and engines, with first-class reverse-order rollback.

> Documentation is written in a later build phase. See `BUILD_SPEC.md` for the
> full specification.
